Some utilities, e.g. implementations of common data structures.

Not necessarily for copy-pasting since it's most beneficial to implement from scratch for every problem, but serves as a good reference.